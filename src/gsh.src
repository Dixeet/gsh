import_code("gsh.index.src")

config = { "debug": {
  "enabled": true,
  "passthrough": false,
} }

gsh.settings += config
debug(gsh.settings, "gsh.settings")

// /////////////////////////////////////////////////////////////////////////////////////////////////
// // TODO: this is for test dont forget to comment
// /////////////////////////////////////////////////////////////////////////////////////////////////
// cmd = "example"
// args = "ship myship move 4 8 --speed=13"

// params = (cmd + " " + args).split(" ")
// /////////////////////////////////////////////////////////////////////////////////////////////////

gsh.core.gshCmd(params)