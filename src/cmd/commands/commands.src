#include "example.src";
#include "exit.src";
#include "clear.src";
#include "cd.src";
commands = function
  commands = {}
  commands.example = example
  commands.exit = exitCmd
  commands.clear = clear
  commands.cd = cd
  return commands
end function