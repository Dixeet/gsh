#include "example.src";
#include "exit.src";
#include "clear.src";
commands = function
  commands = {}
  commands.example = example
  commands.exit = exitCmd
  commands.clear = clear
  return commands
end function