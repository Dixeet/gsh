#include "example.src";
#include "exit.src";
#include "clear.src";
#include "cd.src";
#include "ls.src";
#include "mv.src";
#include "touch.src";
commands = function
  commands = {}
  commands.example = example
  commands.exit = exitCmd
  commands.clear = clear
  commands.cd = cd
  commands.ls = ls
  commands.mv = mv
  commands.touch = touch
  return commands
end function